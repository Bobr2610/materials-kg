You are MiMo working as a bounded coding subagent for Codex.

Repository: C:\MAI\Nornicel\Code\wt-cleanup-runtime
Canonical product: Materials KG / materials knowledge graph.

Read first:
- docs/mimo-runs/20260627/deep-research/20_summary.md
- docs/mimo-runs/20260627/unblock-core/20_summary.md
- README.md
- docs/ARCHITECTURE.md
- kg_engine/api/materials_core.py
- kg_engine/services/materials_kg.py
- kg_engine/config/settings.py

Task slug: cleanup-runtime

Goal:
Make the active public Materials KG runtime free of CMW/Comindware names while keeping old donor/legacy code clearly isolated and out of Materials KG entrypoints.

Hard constraints:
- Do not edit root donor directories cmw-platform-agent or cmw-rag.
- Do not do a broad rewrite of kg_engine/agent or the old kg_engine/api/app.py.
- Do not break the new core API in kg_engine/api/materials_core.py.
- Keep the canonical path domain -> repositories -> services -> api.
- Preserve existing uncommitted graph-first work in this worktree.

Required changes:
1. Public settings/env cleanup:
   - kg_engine/config/settings.py must not expose cmw/comindware fields in the main Settings model.
   - .env.example must not contain CMW_* or Comindware public setup instructions for the Materials KG core.
   - If legacy needs are documented, put them behind an explicit legacy/donor note without making them part of public core settings.

2. Public docs cleanup:
   - README quick start and architecture docs should describe Materials KG, not CMW/Comindware.
   - If legacy folders are mentioned, call them legacy/donor and clearly state they are not public entrypoints for the Materials KG core.

3. Isolation guard:
   - Add or update a focused test or small verification script that scans the active Materials KG surface and fails on cmw|comindware|kb.comindware.ru.
   - The scan should include README.md, pyproject.toml, .env.example, kg_engine/api/materials_core.py, kg_engine/domain, kg_engine/repositories, kg_engine/services, kg_engine/ingestion, kg_engine/scripts/ingest_materials_kg.py, kg_engine/scripts/run_materials_api.py, and focused Materials KG tests.
   - It may explicitly exclude legacy/donor paths such as kg_engine/agent, kg_engine/api/app.py, kg_engine/llm, kg_engine/tools, kg_engine/utils, archived docs/mimo-runs, and root donor directories.

4. Verify:
   - Run the import and ingestion smoke commands that worked in unblock-core.
   - Attempt:
     python -m pytest kg_engine/tests/test_materials_kg_core.py -q
     python -m pytest kg_engine/tests/test_materials_api_smoke.py -q
     python -m pytest kg_engine/tests/test_repository_factory.py -q
     ruff check kg_engine
   - If pytest/ruff are missing in the local Python, record that clearly instead of masking it.

Deliverables:
- Apply the code/doc/test changes in this worktree.
- Write docs/mimo-runs/20260627/cleanup-runtime/20_summary.md with:
  - files changed
  - exact verification commands and results
  - any remaining CMW occurrences and why they are legacy-excluded

Do not commit.

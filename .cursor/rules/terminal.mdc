---
description: Terminal command execution and venv setup for materials-kg
alwaysApply: true
---

## Venv Activation

Always activate venv before running Python. Detect OS first:

- **PowerShell (Windows):** `.venv\Scripts\Activate.ps1`
- **Git Bash / WSL / Linux:** `source .venv/bin/activate`

## Common Dev Commands (after venv)

```bash
# Install
pip install -r kg_engine/requirements.txt

# Test
python -m pytest kg_engine/tests/ -v
python -m pytest kg_engine/tests/test_graph.py -v

# Lint
ruff check kg_engine/
ruff check kg_engine/graph/
ruff check --fix --unsafe-fixes kg_engine/graph/

# Build knowledge graph
python kg_engine/scripts/build_knowledge_graph.py --source chunks.json --output graph.json
python kg_engine/scripts/build_knowledge_graph.py --source chunks.json --output graph.json --use-llm
```

## Agent Files Reference

All 5 agent-facing files in this project must stay consistent. If one is overwritten, check the others:

| File | Purpose |
|------|---------|
| [`AGENTS.md`](../AGENTS.md) | **Source of truth** — full conventions, dev commands, structure |
| [`.agents/skills/materials-knowledge-graph/SKILL.md`](../.agents/skills/materials-knowledge-graph/SKILL.md) | Domain skill — architecture, entity/relation types, query examples |
| [`.cursor/rules/materials-kg-agent.mdc`](./materials-kg-agent.mdc) | Coding rules, domain overview, key patterns |
| `.cursor/rules/terminal.mdc` (this file) | Terminal setup, venv, common commands |
| [`.cursor/rules/commit.mdc`](./commit.mdc) | Commit message format and guidelines |
